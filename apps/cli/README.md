# @ixayldz/nexus-cli

Command line entry point for Nexus-Cli.

```powershell
npm install -g @ixayldz/nexus-cli
nexus --help
```

Live model calls currently target DeepSeek:

```powershell
$env:DEEPSEEK_API_KEY = "..."
nexus exec "Analyze this repository"
```
