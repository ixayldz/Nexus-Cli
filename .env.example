# Provider keys are read from environment variables.
# Do not commit real keys.
DEEPSEEK_API_KEY=

# Optional OpenAI support exists in code but live validation currently targets DeepSeek only.
OPENAI_API_KEY=

# Optional local runtime state override.
NEXUS_HOME=.nexus
