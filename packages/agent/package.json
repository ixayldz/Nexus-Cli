{
  "name": "@nexus/agent",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "package.json"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@nexus/events": "workspace:*",
    "@nexus/model-router": "workspace:*",
    "@nexus/runtime": "workspace:*",
    "@nexus/shared": "workspace:*",
    "@nexus/tool-bus": "workspace:*"
  },
  "devDependencies": {
    "@nexus/config": "workspace:*",
    "@nexus/security": "workspace:*"
  }
}
